<h4 class="text-center">投稿一覧</h4>
<p class="text-center my-3">
  <%= link_to "新しい順", admin_path(latest: "true"), style: "color: #027b71;" %>
  / <%= link_to "古い順", admin_path(old: "true"), style: "color: #027b71;" %>
  / <%= link_to "評価の高い順", admin_path(most_favorites: "true"), style: "color: #027b71;" %>
</p>
<div class="row justify-content-center d-flex flex-wrap mb-5">
  <% if @posts.exists? %>
    <%= render 'index', posts: @posts %>
  <% else %>
    <h6>投稿はされていません</h6>
  <% end %>
</div>
<div class="row justify-content-center mb-5">
  <div class="col-6">
    <%= paginate @posts %>
  </div>
</div>