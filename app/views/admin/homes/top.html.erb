<h4 class="text-center">投稿一覧</h4>
<div class="row">
  <div class="col-12">
    <% if @posts.exists? %>
      <%= render 'index', posts: @posts %>
    <% else %>
      <h6>投稿はされていません</h6>
    <% end %>
  </div>
</div>
