<!--投稿一覧-->
<div class="row">
  <div class="col-12">
    <h4>投稿一覧</h4>
    <% if @posts.exists? %>
      <%= render 'public/posts/index', posts: @posts %>
    <% else %>
      <h6>投稿はされていません</h6>
    <% end %>
  </div>
</div>
