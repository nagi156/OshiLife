<h4 class="text-center mb-3">フォロワー一覧</h4>
<div class="row justify-content-center">
  <div class="col-12 col-md-10 col-lg-6">
    <% if @users.present? %>
      <%= render 'public/users/index', users: @users %>
    <% else %>
      <h6 class="mt-3">フォローはされていません</h6>
    <% end %>
  </div>
</div>