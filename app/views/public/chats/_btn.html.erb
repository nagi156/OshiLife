<% if current_user != user && current_user.followed_by?(user) && user.followed_by?(current_user) %>
  <%= link_to chat_path(user.id), class: "btn btn-outline-secondary" do %>
    DM <i class="fa-solid fa-comments"></i>
  <% end %>
<% end %>