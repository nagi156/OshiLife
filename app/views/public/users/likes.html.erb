<h4 class="text-center mb-3">いいね一覧</h4>
<div class="row justify-content-center d-flex flex-wrap">
  <% if @likes.present? %>
    <%= render 'public/posts/index', posts: @likes_list %>
  <% else %>
    <h6 class="mt-3">いいねはされていません</h6>
  <% end %>
</div>